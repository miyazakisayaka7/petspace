<div class="card">
  <%= link_to image_tag(pet.image, class: :card__imgs ), pet_path(pet.id)%>
  <div class=" card__body">
    <%= link_to pet.name, pet_path(pet.id), class: :card__title%>  
  </div>
</div>